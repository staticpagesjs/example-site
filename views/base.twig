<!DOCTYPE html>
<html lang="{{ lang|default('en') }}">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ title }} – {{ _t.site.title }}</title>
	<link rel="stylesheet" href="{{ url('/assets/main.css') }}">
</head>
<body>
	<header>
		<h1>{{ _t.site.title }}</h1>
		<hr>
	</header>
	<main>
		<aside>
			<nav>
				<h1>{{ _t.nav.title }}</h1>
				<ul>
				{% for item in _t.nav.items %}
					<li><a href="{{ url(item.url) }}.html">{{ item.name }}</a></li>
				{% endfor %}
				</ul>
			</nav>
		</aside>
		<article>
{% block content '' %}
		</article>
	</main>
	<footer>
		<hr>
		<p>Generated by <a href="{{ _t.site.generator.url }}">{{ _t.site.generator.name }}</a> at {{ 'now'|date('Y-m-d H:i:s') }}.</p>
	</footer>
</body>
</html>
